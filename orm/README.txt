*** This plugin is currently under development *** 

It will eventually contain all non-essential Ensembl functionality which requires access to non-genomic databases, including user accounts, news, and the database frontend framework. Display of online help will still be possible without this plugin.

Note that use of this plugin requires installation of the Rose::DB ORM suite and its dependencies, which is why the above-mentioned functionality is being isolated in a plugin.
